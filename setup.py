from setuptools import setup, find_packages

setup(
    name = "eve",
    version = "0.1.0",
    packages = find_packages(),
    install_requires=[
        # more deps here
    ],
    python_requires = ">=3.9",
)
